name: Documentation

on:
  push:
    branches:
      - master
      - new_dev
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r docs/requirements.txt
          python -m pip install -r requirements.txt
          python -m pip install -e .

      - name: Show documentation environment
        run: |
          python --version
          python -m pip list
          python -m sphinx --version

      - name: Verify documentation source files
        run: |
          test -f artist/__plot.py
          test -f docs/source/index.rst
          python -c "import artist.accessor as accessor; print(accessor.ArtAccessor.__name__)"

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Build Sphinx documentation
        run: python -m sphinx -T -b html docs/source docs/build/html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/build/html

  deploy:
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/new_dev'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
